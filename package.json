{
  "name": "event-photo-gallery",
  "version": "1.0.0",
  "description": "Face recognition event photo sorting app",
  "scripts": {
    "start": "bash start.sh",
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "backend": "cd backend && uvicorn server:app --host 0.0.0.0 --port 8000",
    "frontend": "cd frontend && npm start"
  },
  "engines": {
    "node": "20.x"
  },
  "keywords": [
    "face-recognition",
    "event-photos",
    "fastapi",
    "react"
  ]
}
