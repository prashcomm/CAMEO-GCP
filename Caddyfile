{
    local_certs
}

# Catch-all HTTPS block (no specific IP)
# This solves the SNI issue for IP-based access
:443 {
    tls internal
    reverse_proxy app:8000
    
    encode gzip
}

# Catch-all HTTP block for redirection
:80 {
    redir https://{host}{uri}
}
