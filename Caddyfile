{
    local_certs
}

# Explicitly use the VM IP for the TLS block
{$VM_IP}, :443 {
    tls internal
    reverse_proxy app:8000
    
    # Increase compression performance
    encode gzip
}

:80 {
    redir https://{host}{uri}
}
